import { useState } from "react";

function useFeatureCheckboxAnswers() {

    
  const [chosenFeatures, setChosenFeatures] = useState([]);

  const allFeatures = ["colour", "sound", "logo", "size"]
}

export default useFeatureCheckboxAnswers;
